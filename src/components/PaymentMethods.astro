---
import { PAYMENT_METHODS, COMPANY } from '../constants';
---

<section class="py-20 bg-slate-50">
  <div class="container mx-auto px-6 text-center">
    <h2 class="text-3xl md:text-4xl font-bold text-slate-900 mb-4">Métodos de Pago Flexibles</h2>
    <p class="text-slate-600 max-w-2xl mx-auto mb-12">
      Aceptamos todos los medios de pago locales para tu comodidad. Emitimos factura con {COMPANY.taxIdLabel}.
    </p>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-4xl mx-auto">
      {PAYMENT_METHODS.map((method) => (
        <div class="bg-white p-8 rounded-xl shadow-md flex flex-col items-center hover:-translate-y-1 transition-transform duration-300">
           <!-- Placeholder for icons, in a real app these would be SVGs or Image components -->
          <div class="w-16 h-16 bg-slate-100 rounded-full flex items-center justify-center mb-4 text-3xl">
            {method.icon === 'tigo-money' && '📱'}
            {method.icon === 'qr-simple' && '📷'}
            {method.icon === 'bank-transfer' && '🏦'}
          </div>
          <h3 class="text-xl font-bold text-slate-800">{method.name}</h3>
          <p class="text-sm text-slate-500 mt-2">
            {method.name === 'QR Simple' ? 'BCP, BNB, Banco Sol, etc.' : 
             method.name === 'Tigo Money' ? 'Paga desde tu celular' : 'A cualquier banco nacional'}
          </p>
        </div>
      ))}
    </div>

    <div class="mt-12 p-6 bg-yellow-50 border border-yellow-200 rounded-lg inline-block max-w-2xl">
      <p class="text-yellow-800 flex items-center justify-center gap-2">
        <span class="text-2xl">🇧🇴</span>
        Precios unitarios en <strong>Bolivianos</strong>. Sin cargos extra por conversión de moneda.
      </p>
    </div>
  </div>
</section>
